// Legacy client SPA entry (archived)
// Original contents removed from /src/main.tsx to prevent routing conflicts.
// If you need to restore the legacy client, copy this file back to src/ and
// re-install any legacy dependencies.

export {}
